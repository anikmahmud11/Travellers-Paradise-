import React from 'react';
import { Button } from 'react-bootstrap';

const MyOrders = ({order}) => {
    const {  email, address,phone, orders} = order;
    
    return (
        <div>
            
        </div>
    );
};

export default MyOrders;